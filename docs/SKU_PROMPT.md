1. \COPY PASTE toàn bộ prisma model\

2. Tôi đang gặp vấn đề là các item SKU của tôi có `createdById` là null, tôi muốn cập nhật `createdById` của SKU chính là `createdById` của product liên kết với SKU. Hãy viết cho tôi câu lệnh cập nhật bằng SQL.